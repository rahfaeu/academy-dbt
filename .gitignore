venv/
.venv/
target/
dbt_packages/
logs/
.env
.keys/
#Este arquivo serve para configurar o deploy slim CI do dbt para a branch de produção (main) e não pode ser upado para o repositório
#Pois pode gerar problemas em futuros deploys
dbt_deploy_config.json

#File generated by tag.py
tag_adjacency_ordered_matrix.csv

#File necessary for dbt-gloss
.pre-commit-config.yaml

#VScode seetings
.vscode/
.editorconfig

profiles.yml